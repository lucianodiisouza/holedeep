{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and break overlays",
  "windows": ["main", "overlay-*"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
